services:
  openchamber:
    build:
      context: .
      args:
        OPENCHAMBER_VERSION: "${OPENCHAMBER_VERSION:-1.12.3}"
        OPENCODE_VERSION: "${OPENCODE_VERSION:-1.16.2}"
        TTYD_VERSION: "${TTYD_VERSION:-1.7.7}"
    image: "${IMAGE_NAME:-ghcr.io/sbonnick/openchamber-docker}:latest"

  openchamber-ubuntu:
    build:
      context: .
      dockerfile: Dockerfile.ubuntu
      args:
        OPENCHAMBER_VERSION: "${OPENCHAMBER_VERSION:-1.12.3}"
        OPENCODE_VERSION: "${OPENCODE_VERSION:-1.16.2}"
        TTYD_VERSION: "${TTYD_VERSION:-1.7.7}"
    image: "${IMAGE_NAME:-ghcr.io/sbonnick/openchamber-docker}:ubuntu"
